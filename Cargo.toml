[package]
name = "RVOS"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "RVOS"
path = "src/main.rs"
harness = false    # <-- 停用依賴 std 的預設測試框架

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"